[package]
name = "coconut_crab_server"
version = "0.1.0"
authors = ["Michael Jenkins <me@jenkinsmichpa.com>"]
edition = "2024"
description = "Simulated ransomware server for incident response testing"
license = "MIT"
repository = "https://github.com/jenkinsmichpa/coconut_crab"
readme = "../README.md"
keywords = [
  "ransomware",
  "simulation",
  "incident-response",
  "education",
  "security",
]
categories = ["simulation", "security"]

[dependencies]
coconut_crab_lib = { path = "../coconut_crab_lib" }
serde = { workspace = true }
axum = { version = "0.8.9", default-features = false, features = [
  "json",
  "tokio",
] }
axum-embed = "0.1.0"
axum-server = { version = "0.8.0", default-features = false, features = [
  "tls-rustls-no-provider",
] }
env_logger = { workspace = true }
hex = { workspace = true }
log = { workspace = true }
rand = { workspace = true }
purecrypto = { workspace = true, features = ["rsa", "der", "std"] }
rust-embed = { workspace = true, features = ["compression"] }
toasty = { version = "0.10.0", features = ["sqlite", "migration"] }
toasty-driver-sqlite = "0.10.0"
tokio = { version = "1.53.1", features = [
  "macros",
  "rt-multi-thread",
  "signal",
] }
rustls = { version = "0.23.43", default-features = false, features = [
  "ring",
  "std",
  "tls12",
  "logging",
] }

[lints]
workspace = true
